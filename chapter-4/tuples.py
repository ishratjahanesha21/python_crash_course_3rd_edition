t=(1,2,4,5,6,7,8,9,10,)
# name of the tuples variable is t and equal sign and then we store the value in parentheses () separeted the each value using coma .
print(t[8])
dimensions = (200, 50)
# the tuple is immutable we cannot change the value of the tuple but we can reassign a new value to the variable.
for dimension in dimensions:
    print(dimension)
dimensions =(400,100)
for dimension in dimensions:
    print(dimension)
    